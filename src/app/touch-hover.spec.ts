import { TouchHover } from './touch-hover';

describe('TouchHover', () => {
  it('should create an instance', () => {
    const directive = new TouchHover();
    expect(directive).toBeTruthy();
  });
});
